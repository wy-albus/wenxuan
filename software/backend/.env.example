# Prefer environment variables for SMTP secrets. Do not commit a populated .env file.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_user
SMTP_PASSWORD=replace_with_secret
SMTP_FROM=forecast@example.com
SMTP_USE_TLS=true
APP_PUBLIC_BASE_URL=http://127.0.0.1:8000
